node_modules/
dist/
dist-ssr/
.vite/

# secrets — only .env.example and the (empty) Playwright overrides are committed
.env
.env.*
!.env.example
!.env.test

# test artifacts
test-results/
playwright-report/
blob-report/
tests/screenshots/
.playwright/

# editor / os
.DS_Store
*.local
.idea/
.vscode/*
!.vscode/extensions.json
*.log
.freebuff/

# MediaPipe vision assets (~37MB) — fetched by scripts/fetch-vision-assets.mjs
# on npm install, or with `npm run assets`. The runtime falls back to the
# official CDN when they are missing.
public/mediapipe/
public/models/
